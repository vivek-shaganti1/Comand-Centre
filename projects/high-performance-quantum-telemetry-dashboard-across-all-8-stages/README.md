# Quantum Telemetry Dashboard
## Overview
This is a high-performance quantum telemetry dashboard built using Flask and MongoDB.
## Usage
1. Clone the repository
2. Build the Docker image using `docker-compose build`
3. Start the container using `docker-compose up`
4. Access the dashboard at `http://localhost:8080/telemetry`